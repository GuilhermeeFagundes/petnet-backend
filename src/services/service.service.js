import{
    createService
} from "../repository/service.repository"

export const createServiceService = async(serviceData) =>{

    const {
        ser_id,
        ser_nome,
        ser_descricao,
    } = serviceData

}